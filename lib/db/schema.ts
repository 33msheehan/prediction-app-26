// Drizzle schema — PLACEHOLDER.
//
// Tables (users, forecasts, forecast_versions) are defined in T1.2 per §5 of the
// build plan. This file exists now only so drizzle.config.ts has a valid target
// and the migration workflow is wired. Do not treat the absence of tables here
// as the final schema.

export {};
